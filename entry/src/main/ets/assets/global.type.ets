export interface ComponentRouterParams {
  title: string
}
/**
 * 窗口规避尺寸信息
 */
export interface ComAvoidAreaSize{
  topSize: number
  bottomSize: number
  leftSize: number
  rightSize: number
  navigationSize: number
}