interface IBestSignatureColorType {
    bg: ResourceColor
    borderColor: ResourceColor
}

export const IBestSignatureColor: IBestSignatureColorType = {
    bg: $r("app.color.ibest_background_2"),
    borderColor: $r("app.color.ibest_border_color")
}