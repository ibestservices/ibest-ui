import { LINE_HEIGHT, LineHeightData } from './line-height.type';

export const lineHeightData: LineHeightData = {
  lineHeightXs: LINE_HEIGHT.XS as string,
  lineHeightSm: LINE_HEIGHT.SM as string,
  lineHeightMd: LINE_HEIGHT.MD as string,
  lineHeightLg: LINE_HEIGHT.LG as string,
};
