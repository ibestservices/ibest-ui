/**
 * 尺寸设置
 */
export enum SIZE {
	/**
	 * 大尺寸
	 */
	LARGE = 'large',
	/**
	 * 正常尺寸
	 */
	NORMAL = 'normal',
	/**
	 * 小尺寸
	 */
	SMALL = 'small',
	/**
	 * mini尺寸
	 */
	MINI = 'mini',
}
/**
 * 颜色的类型
 */
export type SizeType = `${SIZE}`