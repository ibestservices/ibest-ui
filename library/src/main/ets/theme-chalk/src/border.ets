import { BORDER_RADIUS, BorderRadiusData } from './border.type';

export const borderRadiusData: BorderRadiusData = {
  borderRadiusSm: BORDER_RADIUS.SM as string,
  borderRadiusMd: BORDER_RADIUS.MD as string,
  borderRadiusLg: BORDER_RADIUS.LG as string,
  borderRadiusMax: BORDER_RADIUS.MAX as string,
};
